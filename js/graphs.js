const graphCanvas = document.getElementById("graphs")




var graphFunction = x=>1

function plotGraphs(fun){

    graphCanvas.width = graphCanvas.clientWidth;
    graphCanvas.height = graphCanvas.clientHeight;
    let x = linspace()
    figure("graphs")
    plot

}